import { Interface } from "readline";

export enum Request_For {
  AUTH= 'auth',
  ADMIN = 'admin'
}

export enum API {
  LOGIN = 'login',
  LOGOUT = 'logout',
  REGISTER = 'register',
  PROFILE = 'profile',
  DASHBOARD = 'dashboard'
}

